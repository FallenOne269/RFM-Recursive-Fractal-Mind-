apiVersion: apps/v1
kind: Deployment
metadata:
  name: rfai-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rfai-api
  template:
    metadata:
      labels:
        app: rfai-api
    spec:
      containers:
        - name: rfai-api
          image: rfai-system:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: rfai-secrets
                  key: api-key
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20

apiVersion: v1
kind: Service
metadata:
  name: rfai-system
spec:
  selector:
    app: rfai-system
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
